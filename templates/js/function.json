{
  "memory": 128,
  "timeout": 5,
  "runtime": "nodejs6.10",
  "handler": "lib.default",
  "description": "Example with javascript dependency on an external package",
  "hooks": {
    "clean": "rm -rf ./lib",
    "build": "../../node_modules/.bin/webpack -d --progress --env.dev --bail"
  }
}
